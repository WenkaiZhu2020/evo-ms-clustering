"""Collect SSA-inspired flow evidence for enriched class graph construction."""

from collections.abc import Iterable


def collect_flow_edges(class_names: Iterable[str]) -> list[tuple[str, str, str]]:
    """Placeholder for SSA-inspired inter-class flow evidence."""
    # TODO: Add data/control-flow evidence after the Java analysis format is known.
    _ = list(class_names)
    return []
